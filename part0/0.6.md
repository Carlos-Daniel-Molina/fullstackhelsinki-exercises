# Ejercicio 0.6 - Crear nueva nota desde SPA.

```mermaid
sequenceDiagram
    participant browser
    participant server

Note right of browser: El usuario escribe en el campo de texto para<br>crear una nueva nota<br>y pulsa el botón "Enviar".

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    server-->>browser: 201 Created (JSON con la nueva nota)
    deactivate server

Note right of browser: El navegador actualiza la lista de notas<br>usando el DOM sin recargar nuevamente<br>la página.